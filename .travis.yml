sudo: required

# language: ruby

services:
  - docker

before_install:
  - docker build -t elk .
  - docker run --name elk -d elk
  - docker logs -f elk
  

script:
# - sudo curl http://127.0.0.1:80/
